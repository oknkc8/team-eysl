# 인수인계 — 지금 어디까지 왔나

**다른 머신에서 이어서 작업하기 위한 파일입니다.** git 이력에 남지 않는 것만 적습니다 —
누가 무엇을 들고 있는지, 어떤 마이그레이션 번호가 나갔는지, 무엇을 왜 그렇게 정했는지,
그리고 아직 답을 못 받은 질문들.

**마지막 갱신: 2026-08-31 · `origin/dev` = `123d47e`**

갱신 규칙은 `CLAUDE.md`의 「Handoff log」 절에 있습니다.

---

## 1. 지금 상태

```
dev 머지          56건 · 열린 PR 0건
스크린샷           31장  docs/screenshots/
회원(dev DB)       41명 — 그중 36명이 로그인 없음
기록 258건 · 출석 234행 · 활동 35건
마이그레이션        0050까지 적용·머지
```

**출석 234는 실측이고, 8월 26일의 249보다 15행 적습니다.** 두 측정 모두 잔여물이 없는
값입니다 — 같은 스냅샷의 `with_login`이 양쪽 다 5이고, pwtest 로그인 계정은 여섯이니 스위트가
도는 중이었다면 11이어야 합니다. 분해도 맞습니다: 198→188(10) + 51→46(5) = 15.

**그런데 무엇이 가져갔는지는 밝혀지지 않았습니다.** 한때 `cleanup.sql`의 `marked_by` 팔이
원인으로 지목됐고 그 팔은 `#55`에서 제거됐지만, 그 뒤 출석을 쓰는 스펙을 전부 읽으니 **모두
픽스처 회원을 픽스처 활동에 마킹**합니다. 그 경로는 돈 적이 없고 15행을 설명하지 못합니다.
활동 하나가 지워져 CASCADE로 딸려갔을 가능성이 더 그럴듯한데 확인되지 않았습니다.

**측정은 손실을 증명하지 경로를 증명하지 않습니다.** 손실을 아름답게 설명하는 메커니즘이라도
발동했다는 건 따로 보여야 합니다. 숫자가 반대 방향으로 움직인 것을 지나치지 마십시오 — 이
항목은 그렇게 찾았고, 원인을 지목하는 데서 한 번 틀렸습니다.

**공유 DB라 이 수치는 재는 순간에만 참입니다.** 테스트가 도는 중에 재면 픽스처가 섞여
회원이 42로 보입니다. 이상하면 다시 재십시오.

**되는 것**: 자유게시판 · 성과/배지/메달 · 월간요약 · 다중일 캘린더 · 내 일정 ·
회원 연결 · 영법별 랭킹 · 공지 첨부 · 스태프 대리 참가자 등록 · 결과지 업로드 이력 ·
채팅 첨부 · 대회 신청 · 코치 결과지 접근 · 훈련 상세 · **활동 댓글** · 알림 설정(등록까지)

**방장님 앱에 있고 우리에게 없는 기능은 이제 없습니다.** 활동 댓글이 마지막 격차였고
`#54`에서 닫혔습니다. 남은 차이는 반대 방향 하나뿐입니다 — 활동 취합본을 그분이 `v113`에서
의도적으로 지웠고 우리는 아직 갖고 있습니다(5절 5번).

**안 되는 것**: **알림 발송.** 운영 서버가 없어 VAPID 키가 없습니다. 등록은 되고 보내는
쪽이 안 됩니다. 화면이 그 사실을 직접 표시합니다 — 스크린샷의 알림 화면은 「기능이 도는
증거」가 아닙니다.

---

## 2. 진행 중 — 없음

| 브랜치 | 무엇 | 상태 |
|---|---|---|
| `feat/chat-attachment-name` | `messages.attachment_name`, `0049_a` | **PR #53 머지됨** |
| `chore/e2e-fixture-namespacing` | fixture namespace · 로그인 없는 회원 등록 · signup exact-id cleanup | **PR #58 머지됨** |
| `fix/e2e-teardown-residue` | teardown residue read-only 진단 | **PR #59 머지됨** |
| `test/e2e-screenshot-project` | 일반 E2E와 screenshot 생성 분리 | **PR #60 머지됨** |

**그 외는 전부 머지됐습니다** — 훈련 상세(`#45`) · 라우트 커버리지(`#47`) · 활동 댓글과 푸시
(`#54`) · 채팅 첨부 이름(`#53`) · 정리 결함 둘(`#50`·`#55`) · 문서 넷(`#46`·`#48`·`#52`·`#56`·`#57`).

**열린 PR이 0건이고 미머지 브랜치가 없습니다.** 다음 사람은 `origin/dev`에서 새로 따면 됩니다.

---

## 3. 마이그레이션 번호 대장

**번호를 직접 고르지 마십시오.** 공유 dev DB에서 두 번 충돌했고, 그때마다 파일 이름과
`schema_migrations` 행이 어긋나는 창이 생겼습니다. **중복 번호는 오늘 아무 데서도 오류를
내지 않습니다** — `schema_migrations`가 번호가 아니라 전체 파일명으로 키를 잡기 때문에
`0036_a.sql`과 `0036_b.sql`이 평범한 두 행으로 앉습니다.

```
0044  record_uploads 경로 검증                            적용됨
0045  race_entry                                          적용됨
0046_a 회수된 객체는 쓸리지 않는다                          적용됨
0047  chat_attachments                                    적용됨
0048  훈련 상세          머지됨 · 적용됨
0049_a 채팅 첨부 이름     PR #53 · 적용됨
0050  활동 댓글 + 푸시    머지됨 · 적용됨
0051  다음               비어 있음
```

**`psql -f`로 마이그레이션을 돌리면 대장에 남지 않습니다. 반드시 `migrate.sh`를 쓰십시오.**

`0048`이 한때 이 목록에서 빠져 있었고, 원인이 그것이었습니다. `bash scripts/psql.sh -f
<migration>`은 SQL만 실행합니다. `schema_migrations`에 행을 쓰는 것은 `migrate.sh`이고,
그것도 `--single-transaction`으로 마이그레이션과 대장 행을 함께 커밋합니다.

**그때 검증이 전부 통과한 이유가 여기 있습니다.** 「함수가 있나」와 「병합이 되나」를 물었고
둘 다 참이었습니다. **「대장이 아나」는 묻지 않았습니다.** 객체의 존재와 대장의 기록은 서로
다른 사실인데, 앞의 것으로 뒤의 것을 확인했다고 여긴 것입니다. 이 저장소가 반복해서 밟는
형태 — 검사가 실제로 물은 질문과, 답했다고 생각한 질문이 다른 것.

**적용 순서는 번호 순서가 아닙니다.** 공유 DB라 각자 자기 브랜치 것을 적용하고, 그래서
가운데가 빕니다. 「최고 번호가 N이니 N까지 다 돌았다」는 추론은 여기서 성립하지 않습니다 —
목록을 보고 빠진 것을 찾으십시오.

**번호를 받기 전에 볼 곳 셋.** 로컬 `ls`는 답이 아닙니다 — 한 워크트리는 다른 워크트리의
미푸시 파일을 볼 수 없습니다.

1. `public.schema_migrations` — 이미 적용된 것 전부. dev DB가 공유라 모든 브랜치의 작업이
   여기 모입니다.
2. **모든 원격 브랜치**의 `app/supabase/migrations/`. **`git fetch`를 먼저** 하지 않으면
   피하려던 그 낡은 목록을 다시 읽습니다.
3. `git worktree list` 후 각 디렉터리 — 5분 전에 옆 워크트리에서 쓰이고 아직 푸시 안 된
   파일은 위 둘 **어디에도** 없습니다.

---

## 4. 다음에 할 것 — 순서대로

**지금 열려 있는 항목은 아래 2번 하나뿐이고, 그것도 「재현될 때까지 손대지 않는다」입니다.**
1번은 완료 기록이라 다음 사람이 왜 그렇게 되어 있는지 읽으라고 남깁니다.

**1. 픽스처 격리와 signup cleanup — 완료(`#58`).**

`#22`는 포트를 나눴고 픽스처 id는 나누지 않았습니다. 그래서 지금은 **직렬화지 격리가
아닙니다** — 여러 명이 동시에 e2e를 돌리면 서로의 픽스처를 지웁니다. 실제로 세 번 났습니다.

**접두사를 바꾸지 말고 뒤에 붙이십시오** — `pwtest` → `pwtestA1B2`. `parse.ts`의
`RESERVED_NICKNAME_PREFIX = 'pwtest'`가 `startsWith`로 검사하니 그대로 걸립니다. 갈아치우면
**임포터가 방장님 워크북의 닉네임을 픽스처와 구별하지 못하게 되고, 그것도 조용히** 그렇게
됩니다. namespace가 붙은 `pwtestA1B2회원`도 거절하는 회귀 테스트가 있다.

**uuid는 첫 그룹 꼬리에 넣습니다.** 마지막 그룹은 시드 더미의 인덱스를 담고 있어
(`e0000000-…-000000000007`) 거기를 덮으면 **더미 12개가 하나로 붕괴합니다** — 시드는 성공하고
정원·대기열 테스트만 이상하게 통과하기 시작하므로 조용한 실패입니다.

활동 댓글 fixture까지 namespace를 전파했다. `fixtures.ts`의 `SEED` 값, `seed.sql`의 UUID·닉네임,
댓글의 작성자와 푸시 endpoint 기대값은 한 namespace를 쓴다. 이 항목이 rebase 뒤 빠져 있어
한 번은 27개 signup·댓글 E2E로, 한 번은 전체 E2E로 다시 실행해 잡았다.

**로그인 없는 회원 fixture가 있다.** 명단 추가 테스트는 그 전용 회원을 전용 활동에
추가 → `명단에 있음` 확인 → 다시 빼기까지 한다. 실제 회원을 첫 행으로 고르는 테스트는 금지다.

signup이 만든 계정은 **닉네임·이메일 패턴으로 지우지 않는다.** 성공한 signup은 별도 브라우저로
로그인해 자기 Auth ID와 member ID를 `app/e2e/.auth/owned-signups/`에 기록한다. seed와 teardown은
그 ID만 `cleanup.sql`에 전달한다. ledger가 손상되면 삭제하지 않고 실패한다. 정상 teardown 뒤에는
ledger를 비워 다음 실행에 stale ID를 남기지 않는다. 프로세스가 signup 직후 ledger 기록 전에 죽으면
테스트 데이터가 남을 수 있지만, 실제 회원을 지우는 대신 **안전하게 실패**한 것이므로 ID를 확인해
수동 정리한다.

**검증 (2026-08-31)**: 정적 TypeScript 게이트 4종, Vitest **670개**, namespace된
signup·활동 댓글 E2E **27개**, 전체 Playwright **149개 통과 / 1개 조건부 skip**. 전체 실행은
스크린샷 8장을 현재 namespace 데이터로 갱신했다. 일반 `npm run test:e2e`는 chromium project만
실행해 PNG를 쓰지 않는다. 스크린샷은 `npm run shots`로 명시 실행하고, 이미지가 fixture namespace를
보이므로 커밋 전에는 diff를 확인한다.

**2. 정리가 teardown 없이 끝나는 실행.** 러너 0인데 `attendance`에 pwtest 잔여 15행이 남는
경로는 아직 조사 대상이다. 현재 reporter는 모든 `pwtest%`가 아니라 **자기 namespace만** 세므로
다른 worktree의 정상 실행을 자기 leak으로 오진하지 않는다. `e2e/teardown-residue-check.sql`은
삭제 없이 fixture 회원·활동·출석, fixture 운영진이 실제 데이터에 남긴 cross-boundary 출석,
seed lock 수를 함께 출력한다. 재현 전에는 이 결과를 남기고, lock이 없는 현재 namespace 잔여만
실제 teardown residue로 판단한다. 2026-08-31의 read-only 측정은 다섯 값이 모두 0이었다.

**2026-08-31 측정 — 완주한 실행은 아무것도 남기지 않습니다. 양쪽 끝을 다 쟀습니다.**

이 항목이 오래 재현되지 않은 이유가 여기 있습니다. **시작점이 깨끗하지 않으면 끝점의 숫자가
아무것도 말해주지 않습니다.** 바로 아래 수동 정리로 기준선을 만든 직후에 돌렸기 때문에
「원래 지저분했다」는 대안 설명이 없습니다.

```
실행 전   members=41  pwtest=0  attendance=234  records=258  activities=35
e2e       147 passed  (chromium only, PNG 안 씀 — #60)
실행 후   members=41  pwtest=0  attendance=234  records=258  activities=35  quota_rows=0
```

`signup_attempt_quota`가 0인 것도 같이 나왔습니다. signup 스펙이 실제 가입을 돌리므로 그 표에
행이 쌓이는데, teardown이 거기까지 비웠다는 뜻입니다.

**그리고 중간 상태를 제3자가 독립적으로 봤습니다.** 다른 PR을 검증하던 에이전트가 이 실행이
도는 줄 모른 채 같은 시각에 회원 수를 쟀고, **60명 · pwtest 19~27개 · namespace `dfd671`**을
관측한 뒤 1분 만에 41/0으로 돌아오는 것을 확인했습니다. 우연이지만 값이 있습니다 — 픽스처가
살아 있는 창과 그것이 걷히는 것을 각각 다른 관찰자가 본 셈입니다.

**그래서 남는 설명은 하나입니다.** 정리 로직에는 결함이 없고, 아래 22개는 완주한 실행이 남긴
것이 아니라 **끝을 못 본 실행**이 남긴 것입니다. 「원인이 증명되기 전 cleanup 범위를 넓히지
마라」가 옳았던 것도 이걸로 확인됩니다 — 넓혔다면 실제 회원을 위험에 빠뜨리면서 **아무것도 못
고쳤을** 겁니다.

**세는 검사에 전제가 하나 붙습니다.** 시드 픽스처도 `pwtest` + namespace 형태라 같은 패턴에
걸립니다. 그래서 `count(*) ... like 'pwtest%'`는 **러너가 없을 때만** 0이어야 하고, 워크트리
여러 개가 동시에 도는 환경에서는 그 조건 자체가 드뭅니다. 0이 아닐 때 먼저 물을 것은
「누수인가」가 아니라 **「지금 누가 돌고 있나」**입니다 — `/proc`으로 보십시오, `ps`는 이
환경에서 못 셉니다.

**2026-08-31 관측 — signup 계정 22개가 자동 정리로 닿을 수 없는 상태로 남아 있었습니다.**

```
pwtest 회원          22   전부 네임스페이스 없음 · 전부 로그인 계정 · 3명 approved
그들이 소유한 것       0   출석 · 기록 · 활동 · 게시글 · 신청 전부 0
ledger 항목           0   26개 워크트리 전부에서 비어 있음
```

네임스페이스가 없다는 것이 정체를 말해 줍니다 — **시드 픽스처가 아니라 `signup.spec.ts`가 실제
가입 흐름으로 만든 계정**입니다. 그리고 ledger가 비어 있으므로 **아무 정리도 그들을 이름 댈 수
없습니다.** `cleanup.sql`은 더 이상 닉네임·이메일 패턴으로 지우지 않고, ledger가 유일한
손잡이입니다.

원인은 이 파일이 이미 예상해 둔 그것입니다 — 프로세스가 signup 직후 ledger 기록 전에 죽는 것.
오늘 여러 세션이 사용량 한도로 그렇게 끝났습니다.

**처방대로 exact-ID 수동 정리를 했습니다.** ID 22개를 먼저 파일로 캡처하고, 트랜잭션 안에서
지우되 **실제 회원 수가 41이 아니면 `raise`로 전체를 되돌리도록** 걸었습니다. 목록이 틀렸다면
커밋 자체가 일어나지 않는 형태입니다. 결과는 회원 22 · identity 22 · auth 22 삭제, 가드 통과,
이후 `members=41 no_login=36 with_login=5 attendance=234 records=258 activities=35 pwtest=0`으로
기준선 복귀.

**여기서 알아야 할 것은 구조적 귀결입니다.** ledger는 「정리가 **실제 회원**을 지울 수 있다」를
「정리가 **테스트 회원**을 놓칠 수 있다」로 바꾼 것입니다. 그 거래는 옳습니다 — 두 실패의 값이
비교가 안 됩니다. 다만 **놓친 쪽은 조용히 쌓이고, 이 데이터베이스에는 실제 명단이 같이
있습니다.** 오늘은 3명이 `approved`라 명단 화면에 실제 회원처럼 서 있었습니다.

그래서 **세는 것이 이 설계의 필수 짝입니다.** 러너가 없을 때 이 값은 0이어야 합니다.

```sql
select count(*) from public.members where nickname like 'pwtest%';
```

**0이 아니면 그 자체가 결함이 아니라 「어떤 실행이 끝을 못 봤다」는 신호**이고, 위와 같은
exact-ID 정리로 처리합니다. 패턴으로 지우는 로직을 되살리는 것은 이 거래를 거꾸로 되돌리는
것이라 하지 않습니다.

## Codex 재개 메모 — 2026-08-31

- Claude 세션 `f10128c2-fb0f-408f-8b2a-caab67bcda74`는 사용량 한도로 중단됐다. 이후 작업은
  Codex가 이 branch에서 재개했다.
- 기준은 로컬 루트가 아니라 `origin/dev` `2de71e1`다. 루트 `dev`는 이 기준보다 뒤처져 있을 수
  있으므로, 기존 `free-board` worktree 또는 최신 `origin/dev`에서 새 worktree를 만든다.
- 이 branch는 `origin/dev` 위로 rebase됐다. push 전에는 `git diff --check`, TypeScript 4종,
  Vitest, `npm run test:e2e`를 다시 확인한다. `npm run shots`는 PNG를 쓴다.
- 사용자가 만든 untracked 파일과 git-ignored `.env`는 절대 정리 대상으로 취급하지 않는다.
- 일반 E2E는 screenshot project를 실행하지 않는다. 새 PNG가 필요할 때만 `npm run shots`를 쓰고,
  fixture namespace가 보이는 이미지 diff를 확인한 뒤 커밋 여부를 정한다.

## 5. 방장님께 여쭐 것 — 다섯

우리가 지어낼 수 없어서 남긴 것들입니다.

1. **대회 신청서의 그룹 목록에 남자부가 없습니다.** 「여자 20대 · 여자 일반부 · 여자 30대」
   셋뿐입니다. 일단 그대로 옮기고 남자 회원에게는 안내 문구를 띄웁니다.
2. **레인별 마감인원.** 그분은 자유 텍스트(레인 표현 가능, 숫자 강제 불가), 우리는
   `capacity int`(숫자 강제, 레인 표현 불가).
3. **실격 기록.** 그분 앱에 이 개념이 없습니다. 넣으려면 `result_centiseconds > 0` 불변식을
   깨야 하고, 그걸 쓰는 다섯 경로를 다시 봐야 합니다.
4. **훈련표 파일·사진 첨부.** 폼에 칸은 있는데 **읽는 코드가 없습니다** — `aPlanFile`과
   `aAttach`가 각각 1회, 마크업에서만 나옵니다. 포팅이 아니라 신규 제작입니다.
5. **활동 취합본을 우리도 뺄까요.** 그분이 `v113`에서 **의도적으로 지웠고**, `v114` 패치
   스크립트의 CI가 `grep -q "활동 취합본" && exit 1`로 그걸 강제합니다.

---

## 6. 방장님 upstream — `final93` → `v114`

**작업 방식이 바뀌었습니다.** 사이드카 JS가 13개로 늘었고, `.github` 워크플로와 `scripts`가
생겼습니다.

```
전체 커밋                 130
"Add files via upload"     79
그 외 실제 제목            51
team-eysl-bot 작성          2
```

*(이 절은 한때 「87커밋 중 둘이 비-업로드」라고 적혀 있었습니다. 130/79/51이 실측이고, 「둘」은
비-업로드 커밋 수가 아니라 **봇 커밋 수**였습니다 — 두 수가 섞였습니다.)*

**전환점은 08-26입니다.** 08-23~25에는 66건 중 65건이 업로드였고, **08-27부터 업로드 커밋이
0**입니다. 그러니 CLAUDE.md의 「모든 upstream 커밋은 Add files via upload」는 **우리가 그
문장을 쓰던 날에는 참이었고 08-26에 거짓이 됐습니다.** 규칙을 잴 때는 측정한 날짜를 함께
적어야 하는 이유의 실례입니다.

**사이드카 셋은 죽어 있습니다** — `enhancements-v93` · `notice-fix-v95` ·
`attendance-sync-v104`. `index.html`이 13개 중 어느 것도 참조하지 않고, `sw.js`가 10개만
주입합니다. **파일이 존재한다고 실행되는 게 아닙니다.**

**결함 표가 한 줄 바뀝니다** — 「운영진 출석 체크가 저장 안 됨」이 **고쳐졌습니다.** 다만
그 수정은 `f639033 "Persist admin attendance changes"`가 한 게 아닙니다. 그 커밋은 죽은
사이드카를 추가했을 뿐이고, **진짜 수정은 한참 뒤 봇 커밋 `0149d73`**에서 왔습니다.
커밋 제목으로 결함이 닫혔다고 판단하면 틀립니다.

나머지 다섯은 그대로입니다 — 공지 댓글 덮어쓰기 · 훈련 정원 경합 · 라우터 가드 ·
죽은 jsonb · 과거 훈련 수정 시 출석부 파괴.

---

## 7. 다른 머신에서 시작하기

**새 체크아웃에서 세 가지가 필요합니다.** 하나라도 빠지면 게이트가 이상하게 실패합니다.

```bash
cd app && npm ci          # node_modules 는 따라오지 않습니다
```

**`.env`가 둘입니다** — 둘 다 git-ignored라 새 체크아웃에 없습니다.

- 루트 `.env` — `psql` 스크립트용
- **`app/.env`** — vite·vitest용. **이게 없으면 `vitest`가 첫 단언 전에 죽습니다**
  (`src/lib/env.ts`의 zod 검증이 모듈 로드 시점에 던집니다). **psql이 된다고 vitest가 되는
  게 아닙니다.** 어떤 테스트가 걸리는지는 파일 이름으로 찾지 말고 조건을 만들어 보십시오:
  `mv app/.env app/.env.off && npx vitest run; mv app/.env.off app/.env`

**게이트는 다섯입니다. 하나가 초록이라고 나머지가 초록이 아닙니다.**

```bash
cd app
./node_modules/.bin/tsc --noEmit                    # src
./node_modules/.bin/tsc -p tsconfig.functions.json  # supabase/functions
./node_modules/.bin/tsc -p tsconfig.e2e.json        # e2e
./node_modules/.bin/tsc -p tsconfig.scripts.json    # scripts
./node_modules/.bin/vitest run
npm run test:e2e
npm run shots      # 스크린샷 전용, 기본 실행에서 빠져 있습니다
```

**`npx tsc`를 쓰지 마십시오** — 래퍼가 출력을 「TypeScript compilation completed」로 갈아
치우고 실제 오류를 삼킵니다. `./node_modules/.bin/tsc`를 쓰고 종료 코드를 보십시오.

**dev DB는 공유입니다.** 남의 미머지 마이그레이션이 이미 적용돼 있을 수 있으니, 실패를
조사할 때 `schema_migrations`와 자기 브랜치의 파일 목록을 대조하십시오.

**로컬 메모리는 따라오지 않습니다.** `~/.claude/projects/.../memory/`는 이 머신에만
있습니다. 다른 머신에서 이어받는 데 필요한 것은 전부 이 파일과 `CLAUDE.md`에 있어야 합니다.

---

## 8. 결정된 것 중, 코드만 봐서는 알 수 없는 것

- **첨부 경로를 받는 함수는 서버가 경로를 만들어야 합니다.** 세 모양이 공존합니다 —
  `save_notice_v1`은 서버 유도, `send_message_v1`은 클라이언트가 보내고 검증,
  `record_uploads`는 검증이 없었습니다(`0044`가 닫음). **첫째가 가장 좋습니다. 없는
  파라미터는 틀릴 수가 없습니다.**
- **`activities.details`는 병합해야 합니다**(`||`). 통째 교체가 백필된 출석부를 파괴하는
  방장님 결함의 원인이고, `0048`이 red/green으로 재현했습니다.
- **스토리지 키는 ASCII만.** 한글이 들어가면 Supabase가 `400 InvalidKey`를 냅니다. 읽을 수
  있는 이름은 청구 표의 `file_name` 컬럼에 둡니다 — **키는 식별자, 이름은 데이터.**
  이 결함이 모든 게이트를 통과한 이유는 **버킷에 객체가 0개였기 때문**입니다. 바이트를
  스토리지로 보내 본 테스트가 하나도 없었습니다.
- **e2e 하네스의 락 프로토콜을 손으로 재현하지 마십시오.** 각 조각이 실제 사고 하나씩에
  대응합니다. 줄이면 그 사고가 돌아옵니다 — 이유는 주석에 있습니다.
- **RLS 정책 식은 호출한 롤로 평가됩니다.** `SECURITY DEFINER`는 함수 *본문*의 권한을
  정할 뿐, 호출자의 EXECUTE 필요를 면제하지 않습니다. `0043`이 이걸 놓쳐 앱의 모든 업로드를
  깼고, 마이그레이션은 깨끗하게 적용됐으며 다음 업로드 전까지 아무것도 실패하지 않았습니다.
- **`npm run db:types`는 손으로 고친 것을 조용히 되돌립니다.** `gen-types.sh`는 후처리 없이
  `supabase gen types`의 출력을 파일에 통째로 덮어씁니다. 그래서 **재생성 뒤에는 두 가지를
  되돌려야 합니다.**
  - `member_link_summary_v1` — 어떤 클라이언트 롤에도 EXECUTE가 없어서 일부러 뺍니다.
    타입에 없어야 실수로 호출했을 때 런타임이 아니라 컴파일이 깨집니다.
  - `save_notice_v1`의 `p_notice_id`와 `p_expected_updated_at` — dev에는 `string | null`로
    들어가 있고 생성기는 그렇게 만들지 않습니다. `2ab2153`(#24)에서 손으로 넣은 것입니다.
    생성기는 DEFAULT가 있는 파라미터를 `p_attachment_path?: string`처럼 **옵셔널**로 만들 뿐
    nullable로는 만들지 않습니다. 되돌리지 않으면 `notices/api.ts`의 `?? null` 두 줄이
    깨지고, **그게 마치 `dev`가 레드인 것처럼 보입니다** — 실제로 그렇게 보고된 적이 있습니다.

  **이건 정책이 아니라 두 개의 예외입니다.** 「권한 없는 함수는 다 빼라」로 일반화하지
  마십시오. 세 번째가 생기면 그때 여기에 한 줄 더합니다.
- **정원을 주는 네 경로는 모두 `activities` 행을 먼저 잠급니다.** 2026-08-31에 라이브
  `pg_get_functiondef`로 확인했습니다 — `apply_to_activity` · `activity_enrol_member_v1` ·
  `offer_seat_to_next_waitlister` · `respond_waitlist_offer` 넷 다 `select … from
  public.activities where id = ? for update`가 첫 잠금이고, 그다음에야 `members`나
  `activity_applications`로 갑니다.

  **이 순서가 불변식입니다.** 어느 하나가 `activity_applications`를 먼저 잠그면 나머지 셋과
  잠금 순서가 뒤집혀 교착이 납니다. 그리고 `activities`를 먼저 잡는 덕분에 정원 판정이
  직렬화되어, 스태프 대리 등록과 일반 신청이 같은 자리를 두고 만나도 과예약되지 않습니다 —
  방장님 앱의 「훈련 정원 경합」 결함이 우리 쪽에 없는 이유가 이것입니다.

  **`for update`를 세는 것만으로는 확인되지 않습니다.** 첫 시도에서 정규식이
  `offer_seat_to_next_waitlister`의 주석 한 줄(`FOR UPDATE, unlike 0006…`)을 잠금으로 세어
  순서를 판정하지 못했습니다. 세지 말고 **순서를 읽으십시오.**
- **스크린샷은 문서 재료가 아니라 게이트입니다.** 화면에 적힌 문장은 코드가 아니라서
  **어떤 게이트도 대상으로 삼지 않습니다.** 실제로 두 번, 각자 자기 수정 때문에 화면 문구가
  거짓이 된 것을 스크린샷이 잡았습니다. 타입체크·유닛·diff 리뷰 어느 것도 못 잡습니다.
